# Personal / local-only files (agent persona, editor settings, private templates)
Basilagas21/
.vscode/

# Secrets — never commit real keys (use .env.example with blank values)
.env
.env.*
!.env.example

# Node / Next.js (web app)
node_modules/
.next/
out/
dist/

# Mobile (React Native) — native build output, keeps generic non-Dart patterns
build/
ios/Pods/
android/.gradle/
*.iml

# Python (IoT service / scripts)
__pycache__/
*.py[cod]
.venv/
venv/
*.log

# OS / editor cruft
.DS_Store
Thumbs.db